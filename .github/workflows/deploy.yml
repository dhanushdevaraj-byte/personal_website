# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:name: Deploy to GitHub Pages (via gh-pages branch)

on:
  push:
    branches:
      - personalportfolio

permissions:
  contents: write
  pages: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js (required by deploy action)
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Deploy to `gh-pages` branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          # The action will use a personal token if provided, otherwise falls back to GITHUB_TOKEN.
          github_token: ${{ secrets.GITHUB_TOKEN }}
          personal_token: ${{ secrets.GH_PAGES_TOKEN }}
          publish_dir: .
          publish_branch: gh-pages
  # Triggers the workflow on push or pull request events but only for the "personalportfolio" branch
  push:
    branches: [ "personalportfolio" ]
  pull_request:
    branches: [ "personalportfolio" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
